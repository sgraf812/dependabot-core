[package]
name = "dependabot" # the name of the package
version = "0.1.0"    # the current version, obeying semver
authors = ["support@dependabot.com"]

# This causes Cargo to execute a build.rs file in the same folder
# We generate an empty one
links = "links-1"

[dependencies]
time = "0.1.12"
regex = "0.1.41"
